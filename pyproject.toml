# pyproject.toml

[build-system]
requires      = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["episode"]

[project]
name = "episode"
version = "1.0.0"
readme = "README.md"
dependencies = []
